{
  "name": "smtp-server",
  "version": "1.0.0",
  "description": "A simple smtp server",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "pm2 start src/index.js --name smtp-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "smtp-server": "^3.18.5"
  }
}
